# The tool's own store. It must never reach a remote.
.secretveil/

# What a release build leaves behind.
/dist/
/secretveil
/secretveil-*

# What a test leaves behind.
*.out
*.test

# The npm platform packages are built, not written.
/npm/platforms/
npm/secretveil/LICENSE
node_modules/
